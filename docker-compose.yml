services:
  bayrol-bridge:
    image: ghcr.io/zk35-de/mqtt-bayrol-bridge:latest
    ports:
      - "8883:8883"
      - "8080:8080"
    volumes:
      - ./config.yaml:/config.yaml
      - certs:/mosquitto/certs
    restart: unless-stopped

volumes:
  certs:
